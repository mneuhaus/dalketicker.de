# Session-based (stateful) CSRF protection. Renders real tokens server-side so
# logins/forms work without relying on JavaScript to fill a placeholder token.
framework:
    csrf_protection:
        enabled: true
