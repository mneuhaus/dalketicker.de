security:
    password_hashers:
        Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface: 'auto'

    providers:
        app_users:
            entity:
                class: App\Entity\User
                property: email

    firewalls:
        dev:
            pattern: ^/(_profiler|_wdt|assets|build)/
            security: false
        main:
            lazy: true
            provider: app_users
            form_login:
                login_path: app_login
                check_path: app_login
                enable_csrf: true
                default_target_path: admin_imports
            # Keep the admin logged in across browser restarts. Strictly a
            # functional auth cookie (signed, no tracking), only ever set when
            # the admin logs in — no consent banner needed.
            remember_me:
                secret: '%kernel.secret%'
                lifetime: 2592000 # 30 Tage
                samesite: lax
            logout:
                path: app_logout
                target: event_index

    access_control:
        - { path: ^/admin, roles: ROLE_ADMIN }

when@test:
    security:
        password_hashers:
            Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface:
                algorithm: auto
                cost: 4
                time_cost: 3
                memory_cost: 10
