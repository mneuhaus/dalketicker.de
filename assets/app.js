import './saved_events.js';
import './event_keys.js';
import './filters.js';
import './filter_form.js';
import './surprise.js';
import './share.js';
import './pwa.js';
// app.css is linked directly in base.html.twig (see {% block stylesheets %}),
// not imported here — that keeps a data:-URI CSS module out of the importmap
// (which our CSP would block) and lets the stylesheet apply without FOUC.
